# AI CONTEXT LOADING STRATEGY

Always read:

- AGENTS.md
- APP_CONTEXT
- FRONTEND_ARCHITECTURE
- FEATURE_ARCHITECTURE

Then read task-specific docs and current code.

Before editing a feature inspect its root index, public exports, local components/hooks/types, related API service, route adapter, and tests.

Do not reread unrelated modules or rely on memory of stale code.
