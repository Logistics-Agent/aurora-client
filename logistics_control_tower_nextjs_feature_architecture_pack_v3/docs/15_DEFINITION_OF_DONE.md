# DEFINITION OF DONE

A phase is done when route pages are thin, API calls are service-based, Query manages server state, feature-only files remain local, shared files are actually shared, Tailwind/shadcn follow the design, required loading/error states exist, semantic AI/realtime/permission rules are correct, and validation commands pass.
